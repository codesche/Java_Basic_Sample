import java.util.stream.IntStream;

public class Exercise615 {
    public static void main(String[] args) {
        IntStream numbers = IntStream.empty();
        System.out.println(numbers.count());
    }
}
